line-length = 120
target-version = "py312"

[lint]
select = ["E", "F", "W"]
ignore = [
    "E501",  # line length (handled by formatter)
    "E402",  # module level import not at top of file (used to resolve circular imports)
]
